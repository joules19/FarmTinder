export default interface Product {
    id: number,
    productName: string,
    category: string,
    sku: string,
    unitPrice: string,
    orders: string,
    status: boolean,
    dateAdded: string,
}
